# Changelog

## 2.0.1

- Align README Flutter requirement with pubspec (`>= 3.44.0`).
  README 的 Flutter 版本要求与 pubspec 对齐。

## 2.0.0

- Initial federated release: Media3 Transformer video (H.264 / H.265 MP4,
  no audio) and `ImageDecoder` / `Bitmap` image compression. Accepts
  `content://` input. minSdk 23.
  联合插件首个 Android 实现：Media3 Transformer 视频与 ImageDecoder 图片压缩。
